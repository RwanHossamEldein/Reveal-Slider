name: reveal_slider
description: A customizable, futuristic multi-layer reveal slider and scanner widget for Flutter with smooth transitions and glowing effects.
version: 0.0.1
homepage: https://github.com/RwanHossamEldein/Reveal-Slider
repository: https://github.com/RwanHossamEldein/Reveal-Slider
issue_tracker: https://github.com/RwanHossamEldein/Reveal-Slider/issues

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
